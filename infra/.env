# Non-secret production config for the infra/ compose stack. Committed on
# purpose — there are NO secrets here. Real secrets live in GitHub Actions
# secrets (SSH_*) and in the server's GHCR docker login (~/.docker/config.json).
DOMAIN=atlasfsm.com
ACME_EMAIL=anthonyhudson710@gmail.com
